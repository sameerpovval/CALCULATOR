function clickbtn(number) {
     document.getElementById('inp').value += number;    

   }

   function clrbtn() {
     document.getElementById('inp').value= '';
   }


   function clickequal(){
    let text = document.getElementById('inp').value;
    let result = eval(text);
    document.getElementById("inp").value=result;
 }






       
 /* function clickequal(){
      
    result= document.getElementById('inp');
    expression=result.value;

    const operaters=['*','+','-','/'];

    for( let operater of operaters){
      if (expression.includes(operater)){
        const a =expression.split(operater);
        const num1=parseFloat(a[0]);
        const num2=parseFloat(a[1]);

        if(operater === '+'){
          result.value = num1 + num2;
        }else if(operater === '-'){
          result.value = num1 - num2;
        }else if(operater === '/'){
          result.value = num1 / num2;
        }else if(operater === '*'){
          result.value = num1 * num2;
        }
      }
    }

    
  }
  */

  

 
  





  
 
   






































































































































































































































































































































































































































































































































































































































































































































































































  